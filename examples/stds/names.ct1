(( Base: Any ))

fn(!_unique,&*a,:cls(a));
fn(=, &*a,&b, :set(a,b));

fn(+, &a, &b, :add(a,b));
fn(-, &a, &b, :sub(a,b));

fn(++_unique, &a, :inc(a));
fn(--_unique, &a, :dec(a));

fn(+=, &*a,&b, :a = a + b);
fn(-=, &*a,&b, :a = a - b);
fn(*, &a,&b, :mul(a,b,! _,! _));